-- MTW: handling customizable aliases

mtw.al = {}
mtw_struct.load_external("aliases/handle")
--mtw.loaddefaultaliases() --should be done at some point, but needed when reloading or not?
mtw_struct.load_external("aliases/rogue")
